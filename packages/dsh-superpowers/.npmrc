# Pin the public npm registry. The published package targets it, so installs and
# the committed lockfile resolve against the same source — a machine-wide npmrc
# pointing at a mirror would leak mirror URLs into package-lock.json, and GitHub
# Actions refuses to fetch them (`npm error code EALLOWREMOTE` in `npm ci`).
registry=https://registry.npmjs.org/