# Pin the public npm registry. The published package targets it, so installs,
# the lockfile, and `pnpm publish` all resolve against the same source — the
# machine-wide npmrc points at a mirror, which `publish` cannot write to.
registry=https://registry.npmjs.org/
